{
	"operationId": "getDeadHostConfig",
	"summary": "Get 404 host nginx config",
	"tags": ["404-hosts"],
	"security": [
		{
			"bearerAuth": ["dead_hosts.view"]
		}
	],
	"parameters": [
		{
			"in": "path",
			"name": "hostID",
			"description": "The ID of the 404 Host",
			"schema": {
				"type": "integer",
				"minimum": 1
			},
			"required": true,
			"example": 1
		}
	],
	"responses": {
		"200": {
			"description": "200 response",
			"content": {
				"text/plain": {
					"schema": {
						"type": "string"
					},
					"examples": {
						"default": {
							"value": "server { ... }"
						}
					}
				}
			}
		}
	}
}
