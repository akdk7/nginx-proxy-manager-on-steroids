{% if cors and cors.enabled %}
map $http_origin {{ cors.origin_variable }} {
  {% if cors.allow_all_origins %}
  default $http_origin;
  {% else %}
  default "";
  {% for origin in cors.allow_origins %}
  "{{ origin }}" $http_origin;
  {% endfor %}
  {% endif %}
}
{% endif %}
