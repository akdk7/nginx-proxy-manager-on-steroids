{% if rate_limit_enabled == 1 or rate_limit_enabled == true %}
{% if rate_limit_rps and rate_limit_rps > 0 and rate_limit_zone_name %}
    limit_req zone={{ rate_limit_zone_name }}{% if rate_limit_burst and rate_limit_burst > 0 %} burst={{ rate_limit_burst }}{% endif %}{% if rate_limit_nodelay == 1 or rate_limit_nodelay == true %} nodelay{% endif %};
{% endif %}
{% endif %}
